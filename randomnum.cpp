#include <iostream>
#include <ctime>//generation of pseudo (false) random numbers

int main(){
  srand(time(NULL));
  int num1=(rand() % 6 )+ 1;//6 because i need random numbers between 1-6
  int num1=(rand() % 6 )+ 1;
  int num1=(rand() % 6 )+ 1;
  std::cout<<num;
  std::cout<<num;
  std::cout<<num;
    return 0;
}